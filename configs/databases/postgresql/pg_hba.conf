# PostgreSQL Client Authentication Configuration
# BMAD Studio Template - Security Hardened Configuration

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# IPv4 local connections:
host    all             postgres        127.0.0.1/32            scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             postgres        ::1/128                 scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Replication connections
host    replication     replicator      10.0.0.0/16            scram-sha-256
host    replication     replicator      127.0.0.1/32           scram-sha-256

# Application connections (adjust CIDR blocks for your network)
host    bmad_studio_db  bmad_app_user   10.0.0.0/16            scram-sha-256
host    bmad_studio_db  bmad_readonly   10.0.0.0/16            scram-sha-256

# SSL required connections for production
hostssl bmad_studio_db  bmad_app_user   0.0.0.0/0              scram-sha-256
hostssl bmad_studio_db  bmad_readonly   0.0.0.0/0              scram-sha-256

# Monitoring connections
host    postgres        monitoring      10.0.0.0/16            scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0              reject